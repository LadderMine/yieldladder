[package]
name    = "vault_flex"
version = "0.3.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "20"
shared = { path = "../shared" }

[dev-dependencies]
soroban-sdk = { version = "20", features = ["testutils"] }

