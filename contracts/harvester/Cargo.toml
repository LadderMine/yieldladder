[package]
name    = "harvester"
version = "0.3.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "20"

[dev-dependencies]
soroban-sdk = { version = "20", features = ["testutils"] }

